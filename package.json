{
  "name": "traceity-lp",
  "version": "1.0.0",
  "description": "Traceity Landing Page with Hot Reload",
  "scripts": {
    "test": "node --test tests/branding.test.mjs",
    "dev": "browser-sync start --server --files '**/*.html, **/*.css, **/*.js, assets/**/*' --port 3000 --no-notify --no-open",
    "start": "npm run dev"
  },
  "devDependencies": {
    "browser-sync": "^3.0.2"
  }
}
